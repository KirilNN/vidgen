{
  "name": "@vidgen/events",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts",
    "./bus": "./src/bus.ts",
    "./adapters/memory": "./src/adapters/memory.ts",
    "./adapters/jetstream": "./src/adapters/jetstream.ts"
  },
  "scripts": {
    "lint": "eslint src --max-warnings=0",
    "type-check": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "nats": "^2.29.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
