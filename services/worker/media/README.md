# services/worker/media

Media worker pool — FFmpeg / MLT / Remotion. CPU-heavy.
Capabilities: `ffmpeg`, `mlt`, `remotion`. Task queue: `media`. Profile:
`media`.

See [`services/worker/README.md`](../README.md).
