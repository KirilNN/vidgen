# services/worker/light

Light worker pool — orchestration, REST calls, OAuth refreshes.
Capabilities: `orchestration`, `rest`, `oauth-refresh`. Task queue:
`light`. Profile: `core` (always on; a laptop dev with `core` up gets
this worker for free).

See [`services/worker/README.md`](../README.md) for the full layout
and run instructions.
