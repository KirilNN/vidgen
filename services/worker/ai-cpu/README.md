# services/worker/ai-cpu

AI-CPU worker pool — Whisper.cpp, Piper, Argos, RemBG, LaMa-CPU.
Capabilities: `whisper-cpu`, `piper`, `argos`, `rembg`, `lama-cpu`.
Task queue: `ai-cpu`. Profile: `ai-cpu`.

See [`services/worker/README.md`](../README.md).
