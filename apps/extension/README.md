# @vidgen/extension

Scaffolded by T-001. Implementation lives in later tickets — see `docs/tickets.md`.

## Run

```bash
pnpm --filter @vidgen/extension lint
pnpm --filter @vidgen/extension type-check
```
